networkx
torch
groq
torchvision
torch-summary
triton-windows<3.3 ; sys_platform == "win32"  # torch.compile/Inductor backend for the 48px_exp OCR (pairs with torch 2.6)
ai-edge-litert  # LiteRT/TFLite interpreter for the mocr_tflite OCR
einops
scikit-image
opencv-python
pyclipper
shapely
requests
cryptography
freetype-py
aiohttp
tqdm
deepl
ImageHash
kornia
backports.cached-property
huggingface_hub
transformers==4.46.3  # pinned: transformers 5.x needs torch>=2.7 and breaks manga-ocr
py3langid==0.2.2
sentencepiece
editdistance
numpy==1.26.4
tensorboardX
websockets
protobuf<6.0.0,>=3.20.2 # `google-genai` dependancy requirement
ctranslate2
colorama
openai==1.63.0
tiktoken
httpx==0.27.2 # stop before blocking change in 0.28.0
open_clip_torch
safetensors
pandas
onnxruntime
timm
omegaconf
python-dotenv
nest-asyncio
marshmallow
cython
aioshutil
aiofiles
arabic-reshaper
pyhyphen
langcodes[data]
manga-ocr
langdetect
pykakasi  # furigana (ruby) segmentation for study-mode source text
# pydensecrf disabled on Windows (no C++ build toolchain); refine_mask falls back gracefully.
# pydensecrf@https://github.com/lucasb-eyer/pydensecrf/archive/refs/heads/master.zip
accelerate
bitsandbytes
uvicorn
fastapi
pydantic==2.5.0
python-multipart
google-genai
rich
regex
--extra-index-url https://frederik-uni.github.io/manga-image-translator-rust/python/wheels/simple/
rusty-manga-image-translator
python-bidi
# Rust render engine for render.renderer "shiori". Not on PyPI, so it is pinned to a release
# asset. abi3-py39 (any Python 3.9+) but win_amd64 only — the marker makes pip skip the line
# elsewhere rather than fail the whole install; on other platforms build from the committed
# source: cd shiori-renderer && maturin build --release -o dist && pip install dist/*.whl
shiori-renderer @ https://github.com/hikarin-dev/manga-image-translator/releases/download/aux-runtime-v1/shiori_renderer-0.1.0-cp39-abi3-win_amd64.whl ; sys_platform == "win32" and platform_machine == "AMD64"
# Not a pip package: speech-balloon segmentation weights, from the same release. Without them
# rendering silently drops to the contour fallback, so a node LOOKS fine while producing pages
# that differ from every other node. Download manga109_yolo11n_seg.onnx into models/bubble_seg/.
